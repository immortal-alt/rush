// use crate::clear;

// use crossterm::{
//     event::{self, Event, KeyCode, KeyModifiers},
//     terminal,
// };

// pub fn red(cmd: &[String]) -> std::io::Result<()> {
//     clear!();
//     terminal::enable_raw_mode()?;
//     if cmd.len() == 1 {}

//     loop {

//     }
// }
